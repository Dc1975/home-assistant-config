mm_multimedia_beenden:
  alias: MM Multimedia beenden
  sequence:
  - service: homeassistant.turn_on
    data: {}
    target:
      entity_id: script.mm_beamer_ausschalten
  - wait_template: "{{ is_state('script.mm_beamer_ausschalten', 'off') }}"                      
  - service: homeassistant.turn_on
    data: {}
    target:
      entity_id: script.mm_harmony_off
  mode: single
  icon: mdi:toggle-switch-variant-off