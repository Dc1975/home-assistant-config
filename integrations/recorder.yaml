recorder:
  db_url: !secret mariadb_url
  purge_keep_days: 30
  commit_interval: 20
  exclude:
    domains:
      - camera
      - uptime
      - time_date
    entity_globs:
      - cover.*_curtain
